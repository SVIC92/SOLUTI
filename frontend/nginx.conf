server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Angular usa routing del lado del cliente: cualquier ruta no encontrada cae a index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
