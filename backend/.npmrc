legacy-peer-deps=true
# Render corre `npm install` con NODE_ENV=production, y por defecto eso hace que npm
# omita devDependencies (donde viven `prisma` y `@nestjs/cli`, necesarios para el build:
# `prisma generate` en postinstall y `nest build`). Esto fuerza a incluirlas igual.
include=dev
